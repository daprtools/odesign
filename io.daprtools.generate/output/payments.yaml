apiVersion: dapr.io/v1alpha1
kind: Configuration
metadata:
	name: payments
	namespace: invalid
spec:

	components:
		deny: 
		 - bindings.postgresql

	accessControl: 
		defaultAction: deny
		trustDomain: public


