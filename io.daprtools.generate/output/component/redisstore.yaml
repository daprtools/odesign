apiVersion: dapr.io/v1alpha1
kind: Component
auth:
	secretstore: secretstore
metadata:
	name: redisstore
	namespace: dev
spec:
	type: state.redis
	version: v1
	ignoreErrors: true
	metadata:
		-  name: redisHost
		   secretKeyRef: 
			name: redis-secret 
			key:  redis-password
scopes:
	- app_1
	- app_5

