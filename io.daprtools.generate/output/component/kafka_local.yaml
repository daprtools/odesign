apiVersion: dapr.io/v1alpha1
kind: Component
auth:
	secretstore: secretstore
metadata:
	name: kafka_local
	namespace: dev
spec:
	type: pubsub.kafka
	version: v1
	ignoreErrors: false
	metadata:
		-  name: brokers
		   secretKeyRef: 
			name: kafkabrokers 
			key:  brokerhost

